[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "markey_gcode_parser"
version = "0.1.0"
description = "Fast Rust G-code parser for Markey Phase 2"
requires-python = ">=3.10"

[tool.maturin]
features = ["pyo3/extension-module"]
